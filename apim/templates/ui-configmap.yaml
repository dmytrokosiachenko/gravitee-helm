{{- if .Values.ui.enabled -}}
apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: {{ template "gravitee.name" . }}
    component: "{{ .Values.ui.name }}"
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
  name: {{ template "gravitee.ui.fullname" . }}
data:
  use-gzip: "true"
  gzip-level: "5"
  gzip-types: "*"
  constants.json: |
    {
      "baseURL": {{ template "api.ingress.path.baseurl" . }},
      "management": {
        "title": "{{ .Values.ui.managementTitle }}"
      },
      "documentation": {
        "url": "{{ .Values.ui.documentationLink }}"
      },
      "portal": {
        "entrypoint": {{ template "gateway.ingress.path.entrypoint" . }},
        "title": "{{ .Values.ui.title }}"
        {{- if .Values.ui.portal }}
          {{- range $key, $value := .Values.ui.portal }},
          "{{ $key }}": {{ toJson $value }}
          {{- end -}}
        {{- end }}
      }
      {{- if .Values.ui.theme }},
      "theme": {{ toJson .Values.ui.theme }}
      {{- end }}
      {{- if .Values.ui.scheduler }},
      "scheduler": {{ toJson .Values.ui.scheduler }}
      {{- end }}
    }
{{- end -}}